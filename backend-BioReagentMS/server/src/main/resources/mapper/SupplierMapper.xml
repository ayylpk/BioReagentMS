<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bioreagent.mapper.SupplierMapper">

    <select id="list" parameterType="com.bioreagent.QueryParam.SupplierQueryParm" resultType="com.bioreagent.entity.Supplier">
        SELECT
            id,
            name,
            linkman,
            phone,
            address,
            e_mail
        FROM supplier
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="getById" resultType="com.bioreagent.entity.Supplier">
        SELECT
            id,
            name,
            linkman,
            phone,
            address,
            e_mail
        FROM supplier
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supplier (name, linkman, phone, address, e_mail)
        VALUES (#{name}, #{linkman}, #{phone}, #{address}, #{eMail})
    </insert>

    <update id="update">
        UPDATE supplier
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="linkman != null and linkman != ''">
                linkman = #{linkman},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="eMail != null and eMail != ''">
                e_mail = #{eMail},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM supplier WHERE id = #{id}
    </delete>

</mapper>
