// 切块层共享参数（放这里是为了让 bySection 与 recursive 都能用而不互相 import —— 单一事实源 + 无环）
/**
 * 表格拆分阈值（字符数）。依据：
 *  - md 表一行一条记录，长表（ICSC 的理化/法规表、爬虫台账表）动辄几千行 → 一个 chunk 几十 KB，
 *    超出嵌入模型的语义窗口，向量退化成一个点；
 *  - 1600 ≈ 2×MAX_CHUNK：小表（SDS 一页内的性状表、ICSC 卡）整块保留，行为与改造前一致；
 *  - 按字符而非行数算：行宽差异极大（"暴露限值" 行 vs 长描述行），字符数才是嵌入成本的真尺度。
 */
export const MAX_TABLE_CHARS = 1600
